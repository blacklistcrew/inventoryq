import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
  whiteContainer: {
    marginVertical: 10,
    paddingVertical: 10,
    backgroundColor: "#fff",
  },
});

export default globalStyles;
